<script type="text/javascript" src="/js/svgGauge/gauge.min.js"> </script>
<script src="/js/scripts.js"></script>

{{!-- Insert dashboard-page add script --}}
<script>
    if ($(".dashboard-page").length) {
        console.log("dashboard.js added")
        const item = $(this)
        if (item.length) {
            var script = document.createElement('script');
            script.type = 'text/javascript';
            script.src = '/js/dashboard.js';

            document.getElementsByTagName('head')[0].appendChild(script);
        }
    }
</script>

<script>
    if ($(".team-page").length) {
        console.log("team.js added")
        const item = $(this)
        if (item.length) {
            var script = document.createElement('script');
            script.type = 'text/javascript';
            script.src = '/js/team.js';

            document.getElementsByTagName('head')[0].appendChild(script);
        }
    }
</script>

{{!-- Insert map.js if mapzones partial is detected in template --}}
<script>
    if ($("#zones-list").length) {
        console.log("map.js added")
        const item = $(this)
        if (item.length) {
            var script = document.createElement('script');
            script.type = 'text/javascript';
            script.src = '/js/map.js';

            document.getElementsByTagName('head')[0].appendChild(script);
        }
    }
</script>